### Tips

Try experimenting with the comparison operators in the node REPL which can be access with the node command in vagrant.
Work on your code iteratively - that means in small pieces
To help you figure out how to use `hungry` and `availableTime` inside your function.

```javascript
function whatToDoForLunch (hungry, availableTime){
  console.log ("hungry is", hungry);
}
```